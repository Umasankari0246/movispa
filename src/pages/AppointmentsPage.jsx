import AppointmentsView from '../frontend/AppointmentsPage.jsx'

export default function AppointmentsPage({ clients, setClients }) {
  return <AppointmentsView clients={clients} setClients={setClients} />
}
